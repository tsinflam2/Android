Go to the directory that contain the debug.keystore and enter the folliwing command to obtain the SHA1 KEY:

keytool -list -v -keystore ./debug.keystore -alias androiddebugkey -storepass android -keypass android