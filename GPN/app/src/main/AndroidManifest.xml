<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gpnlimited.gpn" >

    <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- To allow this application to write the file(e.g. photo) into public directory (Including read permission which implicitly allowed) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!--
         The following two permissions are not required to use
         Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAMOERo1A6GnLYwabmnjrP1SECEyT2nBWs" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize|stateVisible" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewsActivity"
            android:label="@string/title_activity_news" />
        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map" />
        <activity
            android:name=".PMActivity"
            android:label="@string/title_activity_pm" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile" />
        <activity
            android:name=".CameraActivityListview"
            android:label="@string/title_activity_camera_activity_listview" >
        </activity>
        <activity
            android:name=".TestCameraRetrieve"
            android:label="@string/title_activity_test_camera_retrieve" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".MainActivity" -->
        <!-- android:label="@string/title_activity_main" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".LoginActivity" -->
        <!-- android:label="@string/title_activity_login" -->
        <!-- android:windowSoftInputMode="adjustResize|stateVisible" > -->
        <!-- </activity> -->
        <activity
            android:name=".CommentActivity"
            android:label="@string/title_activity_comment"
            android:windowSoftInputMode="stateVisible|adjustPan" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".IonImageViewTesting" -->
        <!-- android:label="@string/title_activity_ion_image_view_testing" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".DynamicaalyCreateImageViewTesing" -->
        <!-- android:label="@string/title_activity_dynamicaaly_create_image_view_tesing" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".TestPlaceActivity" -->
        <!-- android:label="@string/title_activity_test_place" > -->
        <!-- </activity> -->
        <!-- <activity -->
        <!-- android:name=".TestUploadActivity" -->
        <!-- android:label="@string/title_activity_test_upload" > -->
        <!-- </activity> -->
        <activity
            android:name=".UploadYoutubeActivity"
            android:label="@string/title_activity_upload_youtube" >
        </activity>
        <!-- <activity -->
        <!-- android:name=".TestIonLoadBitmap" -->
        <!-- android:label="@string/title_activity_test_ion_load_bitmap" > -->
        <!-- </activity> -->
        <activity
            android:name=".MessengerActivity"
            android:label="@string/title_activity_messenger" >
        </activity>
    </application>

</manifest>
